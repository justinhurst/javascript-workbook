'use strict';

const printListOfOlympians = (namesArray) => {
    namesArray.forEach(function(element) {
        console.log(element);
      });
}

printListOfOlympians(['Justin','Sarah','Quincy','Renee','Wade','Dan']);